# SAE study — pinned dependencies (installed on top of the project base env,
# which already provides torch==2.6.0+cu124, transformers==5.7.0, numpy==2.2.6,
# wandb, pandas, tqdm). Install with:  pip install --user -r requirements.txt
#
# Verified import-clean against torch 2.6.0 / CUDA 12.4 on a Tesla V100-32GB
# (sae-lens does NOT pull a different torch — see notebooks/sae/README.md).

sae-lens==6.44.0          # SAE training (StandardTrainingSAE / TopK) + SAE.encode/decode
transformer-lens==3.2.1   # pulled by sae-lens; not used directly for speech encoders
opensmile==2.6.0          # eGeMAPS acoustic descriptors (acoustic-feature labeling)
scikit-learn==1.8.0       # mutual information, logistic-probe re-fit for ablation
